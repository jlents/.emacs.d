(ns command-line-arg.core-test
  (:require [clojure.test :refer :all]
            [command-line-arg.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))

(deftest pairs-of-values
  (let [args ["--server" "localhost"
              "--port" "8080"
              "--environment" "production"]]

))
